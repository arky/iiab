---
language: python
python: "2.7"
before_install:
 - sudo apt-get update -qq
install:
  - pip install ansible==2.6.0
script:
  - echo localhost > inventory
  # Syntax check
  - ansible-playbook --syntax-check -i inventory test/test.yml
# Run role
